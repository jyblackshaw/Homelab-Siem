# Database Setup

Documentation of the MariaDB installation on the DB-Server, remote database architecture for DVWA, user permissions, and the fake data populated for SQL injection practice.

## Sections to Cover
- MariaDB installation and bind-address configuration
- DVWA database and user creation
- Remote connection setup (webserver to DB server across subnets)
- DVWA config.inc.php pointing to remote DB
- Fake data tables (customers, credit cards, employees, medical records, credentials, transactions, admin notes)
- Backup and restore procedures
